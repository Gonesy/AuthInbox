import { Hono } from "hono";import type { AppEnv } from "../types";import { visibleMailById,visibleMails,type MailStatus } from "../services/mail";import { extractMailBodies } from "../services/mime";const mails=new Hono<AppEnv>();mails.get("/",async c=>{const user=c.get("user"),page=Math.max(1,Number.parseInt(c.req.query("page")??"1",10)||1),pageSize=Math.min(100,Math.max(1,Number.parseInt(c.req.query("pageSize")??"20",10)||20)),requested=c.req.query("status")??"all",status:MailStatus=requested==="extracted"||requested==="unprocessed"?requested:"all";const{total,items}=await visibleMails(c.env.DB,user,{toAddr:c.req.query("to_addr")||undefined,service:c.req.query("service")||undefined,status,limit:pageSize,offset:(page-1)*pageSize});return c.json({page,pageSize,total,items});});mails.get("/:id{-?[0-9]+}",async c=>{const id=Number.parseInt(c.req.param("id"),10);if(!Number.isFinite(id))return c.json({error:"Mail not found"},404);const row=await visibleMailById(c.env.DB,c.get("user"),id);if(!row)return c.json({error:"Mail not found"},404);const{textBody,htmlBody}=row.raw?extractMailBodies(row.raw):{textBody:null,htmlBody:null};return c.json({...row,textBody,htmlBody});});export default mails;
